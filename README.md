# Todoeth
Ethereum To Do List

## Local verification

Start a local Truffle blockchain in one terminal:

```sh
npx truffle develop
```

In a second terminal, run the contract regression suite:

```sh
npm test
```
